package platform_test

// testPayload is the canonical content payload used across platform test
// fixtures. Hoisted to a package-level constant to satisfy the goconst linter.
const testPayload = "payload"
